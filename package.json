{
  "name": "texas-business-worth",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply DB --local",
    "db:remote": "wrangler d1 migrations apply DB --remote",
    "check": "node --check src/index.js && node --check src/valuation.js && node --check public/app.js"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
